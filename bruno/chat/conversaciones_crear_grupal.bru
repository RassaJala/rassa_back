meta {
  name: Crear conversación grupal
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/chat/conversaciones/crear-grupal/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
}

body:json {
  {
    "nombre": "Mi grupo"
  }
}
