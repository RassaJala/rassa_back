meta {
  name: Marcar imagen como principal
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/productos/{{productoId}}/imagenes/{{imagenId}}/set-principal/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:json {
  {}
}
