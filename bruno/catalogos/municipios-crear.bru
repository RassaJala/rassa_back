meta {
  name: Municipios - Crear
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/municipios/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
}

body:json {
  {
    "nombre": "Nuevo Municipio"
  }
}

vars:pre-request {
  baseUrl: http://localhost:8000
}
