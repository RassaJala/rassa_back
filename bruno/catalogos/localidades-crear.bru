meta {
  name: Localidades - Crear (ruta anidada)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/municipalities/1/localities/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
}

body:json {
  {
    "nombre": "Nueva Localidad"
  }
}
