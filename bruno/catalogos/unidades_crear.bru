meta {
  name: Crear unidad
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/unidades/
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "nombre": "Kilogramo",
    "abreviatura": "kg",
    "estado": true
  }
}
